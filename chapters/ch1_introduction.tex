\section{Introduction}
\label{sec:ch1}

\begin{definition}
  \label{def:hom}

  Let $G$ be a graph. A set $S \subseteq V(G)$ is \textit{homogeneous} if it induces a clique or an independent set in $G$.
\end{definition}

\begin{theorem}[Erdős--Szemerédi \cite{erdos1972ramsey}]
  \label{thm:ramsey-density}

  Let $G$ be a graph on $n$ vertices with less than $dn^2$ edges. Then $G$ contains a homogeneous set of size greater than $\frac{c}{d\log(1/d)} \log n$. 
\end{theorem}

\begin{proof}
  Note that there exists $m \geq n/2$ vertices $v_1, v_2, \ldots, v_m$ with $\deg(v_i) \leq 2dn$. Let $H$ be the graph induced by these vertices. Rearranging 
  
  Let $k$ be the size of the largest independent set in $H$ 
\end{proof}

\begin{corollary}
  \label{cor:ramsey-density}
  For any $C$-Ramsey graph $G$, there exists $\delta = \delta(C) \in (0, 1/2)$ such that the edge density of $G$ lies in the interval $[\delta, 1-\delta]$.
\end{corollary}

\begin{proof}
  Apply Theorem \ref{thm:ramsey-density} to $G$ and $\overline{G}$. 
\end{proof}