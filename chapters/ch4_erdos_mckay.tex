\section{The Erd\H{o}s--McKay Conjecture}
\label{sec:ch4}

Both proofs in Chapters~2 and~3 rely on the same anticoncentration principle: when the symmetric difference $|N(x) \triangle N(y)|$ is large, the degree difference $\deg_U(x) - \deg_U(y)$ is a sum of many independent bounded random variables, and its point probabilities are $O(1/\sqrt{n})$. The Erd\H{o}s--McKay conjecture asks for a qualitatively stronger result that asserts $\Phi(G)$ contains every integer in a quadratic-length interval, not merely that it is large. The conjecture was resolved by Kwan, Sah, Sauermann, and Sawhney~\cite{Kwan_2023}, and this chapter is dedicated to outlining their proof. 
\begin{theorem}[Erd\H{o}s--McKay conjecture
  {\cite{Kwan_2023}}]
  \label{thm:em}
  Fix $C > 0$. Let $G$ be a $C$-Ramsey graph on $n$ vertices for sufficiently large $n$. Then there exists $\delta = \delta(C) > 0$ such that
  \[
    \Phi(G) \supseteq \bigl\{1, 2, \ldots, \delta e(G)\bigr\}.
  \]
\end{theorem}

The approach to this theorem fundamentally shifts from bounding degree-collision probabilities for pairs of vertices to directly controlling the point probabilities of $e(U)$ itself.

\subsection{Point probabilities of $e(U)$}

Let $G$ be a Ramsey graph on $[n]$, and let $U$ be a uniformly random vertex subset of $G$. By the Erd\H{o}s--Szemer\'{e}di bound (Corollary~\ref{cor:ramsey-density}), the random variable $e(U)$ has mean $\mu \coloneqq \mathbb{E}[e(U)] = e(G)/4 = \Theta(n^2)$. For variance, the standard variance identity gives
\[
  \Var\bigl(e(U)\bigr) = \sum_{(e, e') \in E(G)^2} \Cov\bigl(\mathbbm{1}_{e \subseteq U}, \mathbbm{1}_{e' \subseteq U}\bigr).
\]
The covariance is $0$ if $e$ and $e'$ are disjoint and $\Theta(1)$ otherwise, so $\Var(e(U)) = \Theta(n^3)$. For a fixed vertex $v$, the number of pairs of edges sharing $v$ is $\binom{\deg(v)}{2}$, and the Erd\H{o}s--Szemer\'{e}di bound with Jensen's inequality gives
\[
  \Var\bigl(e(U)\bigr) \gtrsim \sum_{v \in V(G)} \binom{\deg(v)}{2} \geq n\binom{2e(G)/n}{2} = \Omega(n^3).
\]
Thus the standard deviation is $\sigma \coloneqq \sigma(e(U)) = \Theta(n^{3/2})$. 

In Chapters~2 and~3, the anticoncentration analysis targeted the degree difference $\deg_U(x) - \deg_U(y)$, which is approximately normally distributed with standard deviation $\Theta(\sqrt{n})$, and the control of $e(U)$ was achieved indirectly through the construction of $\mathbf{S}, \mathbf{T}, \mathbf{X}$. It turns out that, via standard techniques, a CLT directly holds for $e(U)$, giving us
\[
  \prob\bigl[e(U) \leq x\bigr] = \Phi\left(\frac{x - \mu}{\sigma}\right) + o(1/\sigma)
\]
for all $x \in \R$ with $\Phi$ being the standard Gaussian cumulative distribution function. In~\cite{Kwan_2023}, it is strengthened to an approximation of the local CLT, showing that the point probabilities of $e(U)$ are all of order $\Theta(1/\sigma) = \Theta(n^{-3/2})$ within the bulk of the distribution.

\begin{theorem}[Kwan--Sah--Sauermann--Sawhney
  {\cite{Kwan_2023}}]
  \label{thm:ksss}
  Fix constant $C > 0$. Let $G$ be a $C$-Ramsey graph on $n$ vertices and let $U$ be a uniformly random subset of $V(G)$. Then
  \[
    \sup_{x \in \Z} \prob[e(U) = x]
    = O(n^{-3/2}),
  \]
  and for every fixed $A > 0$,
  \[
    \inf_{\substack{x \in \Z \\
      |x - e(G)/4| \leq A n^{3/2}}}
    \prob[e(U) = x]
    = \Omega(n^{-3/2}).
  \]
\end{theorem}

The Erd\H{o}s--McKay conjecture follows directly from the lower bound of Theorem~\ref{thm:ksss} combined with Theorem~\ref{thm:alks2003} on small values of $x$.

\paragraph{Proof of Theorem \ref{thm:em}.} 
By Theorem~\ref{thm:alks2003}, there exists $\alpha = \alpha(C) > 0$ such that $\Phi(G) \supseteq \{0, 1, \ldots, n^\alpha\}$. Fix integer $x$ between $n^\alpha$ and $e(G)/4$. Let $m$ be the smallest integer such that $e(\{1, \ldots, m\}) \geq 4x$. Put $G' \coloneqq G[\{1, \ldots, m\}]$ and note that 
\[
  e(G') \geq 4x \geq e\bigl(\{1, \ldots, m-1\}\bigr) \geq e(G') - m,
\]
so $|x - e(G')/4| \leq m/4 \leq m^{3/2}$. Since $m^2 \geq e(G') \geq 4x \geq n^\alpha$, we have $m \geq n^{\alpha/2}$, so $G'$ is $(2C/\alpha)$-Ramsey. Applying Theorem~\ref{thm:ksss} to $G'$ with $A = 1$ gives
\[
  \prob\bigl[e(G'[U]) = x\bigr] = \Omega(m^{-3/2}) = \Omega(n^{-3\alpha/4}) > 0.
\]
Therefore, there exists $U \subseteq V(G') \subseteq V(G)$ such that $e(G[U]) = e(G'[U]) = x$, for sufficiently large $n$. This completes the proof. \qed

\textit{Remark:} Kwan, Sah, Sauermann, and Sawhney~\cite{Kwan_2023} prove the stronger statement $\Phi(G) \supseteq \{0, 1, \ldots, (1 - \eta) e(G)\}$ for any $\eta > 0$, at the cost of a slight complication in the argument. We settle for the weaker version that suffices for the conjecture.

We will outline the proof of Theorem~\ref{thm:ksss} in the remainder of this chapter.

\subsection{Proof overview}
\label{sec:proof-overview}

The standard way to approach local limit theorems is via Fourier transforms. Define the \emph{graph polynomial} as
\[
  f(\xi_1, \ldots, \xi_n) \coloneqq \sum_{ij \in E(G)} \xi_i \xi_j.
\]
For any $U \subseteq V(G)$, setting $\xi_v \coloneqq \mathbbm{1}_{v \in U}$ gives $f\bigl(\vec{\xi}^{\,(U)}\bigr) = e(U)$. The Erd\H{o}s--McKay conjecture is thus equivalent to asking whether the range of this \textit{quadratic} polynomial on $\{0,1\}^n$ covers every integer in $\{0, \ldots, \delta n^2\}$.

For a random variable $X$, the \textit{characteristic function} of $X$ is defined as
\[
\varphi_X(\tau) \coloneqq \mathbb{E} \left[e^{i\tau X}\right] = \int_{-\infty}^{\infty} e^{i\tau X} \cdot \prob[X = x] \, dx, \qquad \tau \in \mathbb{R},
\]
and note that $|\varphi_X(\tau)| \leq 1$ for all $\tau$. Applying the Fourier inversion formula yields
\begin{equation}
\label{eq:fourier-inversion}
\prob[X = x] = \frac{1}{2\pi}\int_{-\pi}^{\pi} e^{-i\tau x}  \varphi_X(\tau) \, d\tau \leq \frac{1}{2\pi}\int_{-\pi}^{\pi} |\varphi_X(\tau)| \, d\tau,
\end{equation}
for integer-valued $X$. In particular, any upper bound on the integrated $|\varphi_X(\tau)|$ produces an upper bound on the point probability. For the matching lower bound, we compare $\varphi_X$ with the characteristic function $\varphi_Z(\tau) \coloneqq e^{i\tau\mu - \sigma^2\tau^2/2}$ of a Gaussian $Z \sim \mathcal{N}(\mu, \sigma^2)$, where $\mu = \mathbb{E}[X]$ and $\sigma^2 = \mathrm{Var}(X)$. Since the Gaussian density at $x$ is of order $\Theta(1/\sigma)$ for any $x$ within $O(\sigma)$ of the mean, $\prob[X = x]$ inherits the same order of magnitude $\Theta(1/\sigma)$ through \eqref{eq:fourier-inversion} whenever $|\varphi_X(\tau) - \varphi_Z(\tau)|$ is small. If a local CLT held for $X = e(U)$, the proof would amount to verifying suitable estimates on $|\varphi_X(\tau)|$ on $[-\pi, \pi]$ and we are done.

Unfortunately, an actual local CLT does not hold for $e(U)$ in general. The distribution of $e(U)$ can fail to be smooth at the unit scale despite the global shape being approximately Gaussian, which results in spikes in the characteristic function and prevents the integrand in \eqref{eq:fourier-inversion} from being uniformly small. In particular, the authors in~\cite{Kwan_2023} observed a ``two-scale'' behaviour of the distribution of $e(U)$, depending on the additive structure of the degree sequence of $G$ (see Figure~\ref{fig:structured}).

\begin{figure}[h]
  \centering
  \includegraphics[width=\textwidth]{figures/structured.png}
  \caption{Histograms of $e(U)$ obtained from ten million independent samples of $U$ from an outcome of $G_{1000, 0.8}$. We see that on the large scale, the distribution of $e(U)$ is approximately Gaussian, whereas on the small scale it shows multiple smaller Gaussian-like curves. Experiment reproduced from~\cite{Kwan_2023}; code in Appendix~\ref{app:two-scale-code}.}
  \label{fig:structured}
\end{figure}

The proof of Theorem~\ref{thm:ksss} overcomes this obstacle in two stages. The first stage splits into two cases according to whether the degree sequence of $G$ is additively structured or not. The \textit{unstructured case} can be handled by a standard Fourier-analytic argument without further intervention. The \textit{structured case} is where $|\varphi_X(\tau)|$ spikes, which we will dedicate most of the effort to handling. These two cases yield the interval estimate 
\begin{equation}
\label{eq:interval-estimate}
\prob\bigl[|X - x| \leq B\bigr] = \Theta(n^{-3/2}),
\end{equation}
for some $B = B(C)$. The second stage upgrades~\eqref{eq:interval-estimate} to a pointwise estimate by showing that the probability mass cannot be too unevenly distributed within the interval via a \textit{switching argument}.

\subsection{Degree sequence dichotomy}
\label{sec:fourier-walsh}

We first decompose $e(U)$ into a sum of a linear and quadratic part. Set $\xi_v \coloneqq \mathbbm{1}_{v \in U}$ and write $x_v \coloneqq 2\xi_v - 1 \in \{-1, +1\}$, so that $\xi_v = (1 + x_v)/2$. Substituting into $e(U) = f\bigl(\vec\xi\bigr) = \sum_{ij \in E(G)} \xi_i \xi_j$,
\begin{equation}\label{eq:decomposition}
  X = \sum_{ij \in E(G)} \frac{(1 + x_i)(1 + x_j)}{4} = \underbrace{\frac{e(G)}{4}}_{\mathbb{E}[X]} + \underbrace{\frac{1}{4}\sum_{v \in V(G)} \deg(v) x_v}_{L} + \underbrace{\frac{1}{4}\sum_{uv \in E(G)} x_u x_v}_{Q},
\end{equation}
and denote the linear term as $L$ and the quadratic term as $Q$. Since $\mathbb{E}[x_v] = 0$ and $\mathbb{E}[x_u x_v] = 0$ for $u \ne v$, we have $\mathbb{E}[L] = \mathbb{E}[Q] = 0$, the covariance of $L$ and $Q$ is zero, and the constant term $e(G)/4 = \mathbb{E}[X]$. It can be shown via the Erd\H{o}s--Szemer\'{e}di bound and Jensen's inequality that $\Var(L) = \Theta(n^3), \Var(Q) = \Theta(n^2)$.
% Note that $\Var(L) = \frac{1}{16}\sum_{v}\deg(v)^2$ and the Erd\H{o}s--Szemer\'{e}di bound gives $e(G) = \Theta(n^2)$, and the Jensen's inequality and handshake lemma yield
% \[
%   \Omega(n^3) = \frac{(2e(G))^2}{n} = \frac{1}{n}\left(\sum_{v}\deg(v)\right)^2 \leq \sum_{v}\deg(v)^2 \leq n\sum_{v}\deg(v) = O(n^3),
% \]
% which shows that $\Var(L) = \Theta(n^3)$. On the other hand, it is straightforward to see that $\mathbb{E}[x_u x_v x_{u'} x_{v'}] = 1$ when $\{u, v\} = \{u', v'\}$ and 0 otherwise, so the Erd\H{o}s--Szemer\'{e}di bound gives
% \[
%   \Var(Q) = \mathbb{E}[Q^2] = \frac{1}{16}\sum_{uv, u'v' \in E(G)} \mathbb{E}[x_u x_v x_{u'} x_{v'}] = \frac{e(G)}{16} = \Theta(n^2).
% \]
The linear part $L$ therefore dominates the total variance, so we expect $L$ to drive the global behaviour of $X$ and $Q$ to act as a corrective role. Write $d_v \coloneqq \deg(v)/4$ so that $L = \sum_{v} d_v x_v$. Since the variables $x_v$ are independent,
\begin{equation}
  \label{eq:phi-L}
  \varphi_L(\tau) = \mathbb{E}\left[\exp\left(i\tau\sum_v d_v x_v\right)\right] = \prod_{v \in V(G)} \mathbb{E}\bigl[\exp(i\tau d_v x_v)\bigr] = \prod_{v \in V(G)} \cos\bigl(\tau d_v\bigr). 
\end{equation}
The decomposition \eqref{eq:decomposition} thus allows us to bound $X$ by bounding the characteristic function of $L$ which factors well. 

For $y \in \R$, denote $\|y\| \coloneqq \mathrm{dist}(y, \mathbb{Z})$ as the distance from $y$ to the nearest integer, and note that $0 \leq \|y\| \leq 1/2$. By Jordan's inequality and $1 - t \leq \exp(-t)$ for $t \geq 0$, 
\[
  |\cos(\pi y)| = \cos(\pi \|y\|) = \sqrt{1 \!- \sin^2(\pi \|y\|)} \leq \sqrt{1 \!-\! 4\|y\|^2} \leq 1 - 2\|y\|^2 \leq \exp(-2\|y\|^2),
\]
for $y \in \R$, so applying to \eqref{eq:phi-L} gives
\begin{equation}
\label{eq:phi-L-bound}
|\varphi_L(\tau)| \leq \exp\left(-2\sum_{v \in V(G)} \left\|\frac{\tau d_v}{\pi}\right\|^2\right).
\end{equation}
The right-hand side decays exponentially in $n$ unless many of the quantities $\tau d_v/\pi$ are simultaneously close to integers. Throughout what follows, let $\nu = \nu(C) > 0$ be a small constant. The low range $|\tau| \leq n^{-0.99}$ is controlled by comparison with the characteristic function of a Gaussian $\varphi_Z(\tau) \coloneqq \exp\bigl(i \mu \tau - \sigma^2 \tau^2/2\bigr)$ via a version of the Esseen's inequality~\cite[Lemma 6.1]{Kwan_2023}. The high range $|\tau| \geq \nu$ is controlled by a \textit{quadratic-cancellation} estimate on $Q$~\cite[Section 8]{Kwan_2023}. What concerns us is the medium window $n^{-0.99} \leq |\tau| \leq \nu$, in which the bound~\eqref{eq:phi-L-bound} splits into two cases. 

\paragraph{The unstructured case.} In the case that
\[
  \sum_{v \in V(G)} \left\|\frac{\tau d_v}{\pi}\right\|^2 = \omega(\log n)
\]
for all $n^{-0.99} < |\tau| \leq \nu$, the bound \eqref{eq:phi-L-bound} gives a superpolynomial decay $|\varphi_L(\tau)| \leq n^{-\omega(1)}$ on the window. In this case, Esseen's inequality applied to the difference $\varphi_X - \varphi_Z$ delivers the interval estimate \eqref{eq:interval-estimate}. This case admits a local CLT.

\paragraph{The structured case.} Here we consider the opposite case that
\[
  \sum_{v \in V(G)} \left\|\frac{\tau d_v}{\pi}\right\|^2 = O(\log n),
\]
for some $n^{-0.99} < |\tau| \leq \nu$. This says that for this one frequency, many of the numbers $\tau d_v/\pi$ are simultaneously close to integers. Lemma~4.12 of~\cite{Kwan_2023} uses this condition to show that for small enough $\gamma > 0$, the vertex set admits a partition
\begin{equation}
  \label{eq:partition}
  V(G) = R \sqcup (B_1 \sqcup \cdots \sqcup B_m),
\end{equation}
with $|R| \leq n^{1-\gamma}$, $|B_j| = \lceil n^{1-2\gamma}\rceil$ for $j \in [m]$, and real numbers $\kappa_1, \dots, \kappa_m \geq 0$ such that
\[
  |d_v - \kappa_j| \leq n^{1/2 + 4\gamma},
\]
for $j \in [m]$ and $v \in B_j$. That is, the vertex set splits into $m = \Theta(n^{2\gamma})$ many buckets $B_1, \dots, B_m$, each with degrees clustered around $\kappa_j$, and a sublinear-sized exceptional set $R$. This allows us to write $L$ as
\[
 L = \sum_{j = 1}^m \sum_{v \in B_j} d_vx_v + \sum_{v \in R} d_v x_v = \underbrace{\sum_{j = 1}^m \kappa_j \sum_{v \in B_j} x_v}_{L_{\text{main}}} + \underbrace{\sum_{j = 1}^m \sum_{v \in B_j} (d_v - \kappa_j)x_v}_{L_{\text{err}}} + \underbrace{\sum_{v \in R} d_v x_v}_{L_{R}}.
\]
Then conditional on
\[
  \vec\Delta \coloneqq \bigl(|U \cap B_1|, \dots, |U \cap B_m|, (x_v)_{v \in R}\bigr),
\]
namely the sizes of each bucket's intersection with $U$ and $(x_v)_{v \in R}$, makes $L_{\text{main}}$ and $L_{R}$ deterministic, reducing the conditional variance of $L$ to $\Var(L_{\text{err}} \mid \vec\Delta) \leq n^{2 + 8\gamma}$. The effect of conditioning on $\vec\Delta$ can be thought of as conditioning on the approximate value of $L$ which governs the large-scale behaviour of $X$, allowing us to ``zoom in'' to see the smaller-scale behaviour of the distribution which corresponds to one of the peaks in Figure~\ref{fig:structured}. In particular, $U \setminus R$ is conditionally uniform over the product of Boolean slices
\[
  \binom{B_1}{\Delta_1} \times \cdots \times \binom{B_m}{\Delta_m}.
\]
This reduces the interval estimate \eqref{eq:interval-estimate} to evaluating the small-ball probability of $L_{\text{err}} + Q$ on this product of Boolean slices.

% \textit{Remark.} The dichotomy above is an instance of the inverse Littlewood--Offord phenomenon. In Chapter~\ref{sec:ch3} the forward direction of Littlewood--Offord controlled the collision probability $\Pr[\deg_U(x) = \deg_U(y)]$ via the large symmetric difference $|N(x) \triangle N(y)|$. Here the forward direction would tell us that $L = \sum_v d_v x_v$ has small-ball probability $O(n^{-3/2})$ provided the coefficient vector $(d_v)$ is generic. The inverse direction, developed by Tao and Vu and by Rudelson and Vershynin, identifies the exceptional case as precisely additive structure, which Lemma~4.12 of \cite{Kwan_2023} in turn converts into the bucket partition we used in the structured case. The formal threshold at which the inverse case kicks in is controlled by the \emph{regularized least common denominator} of $(d_v)$, a notion introduced by Vershynin in random matrix theory, and we do not require the formal definition here.

It is tempting at this point to hope that, after conditional on $\vec\Delta$, $L_{\text{err}} + Q$ satisfies a local CLT of its own. This hope is too optimistic. Consider $G$ to be the disjoint union of two independent copies of $G(n/2, 1/2)$. With high probability $G$ is a Ramsey graph. Every vertex has degree approximately $n/4$, so all vertices fall into a single bucket and $\vec\Delta = |U|$. However, conditional on $|U| = n/2$ yields a normalized limiting distribution of the form $\alpha Z_1^2 + \beta Z_2$ for independent Gaussians $Z_1, Z_2$. Therefore, the asymptotic conditional distribution of $X$ need not be Gaussian after conditional on the bucket, though we shall see that it can always be evaluated at independent Gaussians.

% Consider $G$ to be the disjoint union of two independent copies of $G(n/2, 1/2)$, with vertex sets $V_1$ and $V_2$. With high probability $G$ is a Ramsey graph. Every vertex has degree approximately $n/4$, so all vertices fall into a single bucket and $\vec\Delta = |U|$. Conditioning on $|U| = n/2$ and writing $s = |U \cap V_1|$,
% \[
%   \E[X\mid s] = \frac{1}{2}\binom{s}{2} + \frac{1}{2}\binom{n/2 - s}{2} = \frac{1}{2}(s - n/4)^2 + \frac{n^2}{32} - \frac{n}{8}.
% \]
% The variable $s$ is hypergeometric with mean $n/4$ and variance approximately $n/16$, so by the hypergeometric CLT, $(s - n/4)/(\sqrt{n}/4) \to Z_1 \sim \mathcal{N}(0, 1)$ in distribution. In particular, $(s - n/4)^2$ contributes a term of order $n$ to $X - \E[X]$ with a scaled limit of $\alpha Z_1^2$ for some $\alpha > 0$. The remaining randomness in $X$ comes from the random distribution of the edges in the two copies of $G(n/2, 1/2)$. This is a sum of $\Theta(n^2)$ independent Bernoulli random variables, which also contributes a term of order $n$ and has a scaled limit of a Gaussian $\beta Z_2$ for some $\beta > 0$. Thus, conditioning on $|U| = n/2$, 
% \[
%   \frac{X-\E[X]}{\sigma}\stackrel{d}{\longrightarrow}\alpha Z_1^2+\beta Z_2,
% \]
% which is not Gaussian. Therefore, the asymptotic conditional distribution of $X$ need not be Gaussian after the bucket conditioning, though we shall see that it can always be evaluated at independent Gaussians.

\subsection{The structured case}
\label{sec:robust-rank}

The target now is to show
\begin{equation}
\label{eq:conditional-target}
  \prob\left[|X - x| \leq B \mid \vec\Delta\right] = O(n^{-1}),
\end{equation}
which gives the interval estimate~\eqref{eq:interval-estimate} after averaging over $\vec\Delta$. Specifically, since $\sigma\bigl(\E[X \mid \vec \Delta]\bigr) = \Theta(n^{3/2})$, only $O(n/n^{3/2}) = O(n^{-1/2})$ fraction of $\vec \Delta$ values place $\E[X \mid \vec\Delta]$ within $O(n)$ of $x$, so this fraction contributes $O(n^{-1/2}) \cdot O(n^{-1}) = O(n^{-3/2})$ when averaged over $\vec\Delta$. The remaining tail values of $\vec \Delta$ are negligible by \textit{hypercontractivity}~\cite[Theorem 4.15]{Kwan_2023}. 

As shown in the previous section, the conditional distribution of $X$ need not be approximately Gaussian, so the standard Fourier-analytic approach does not close on its own. The authors of~\cite{Kwan_2023} overcome this by combining the following three ingredients.

\paragraph{Ingredient 1. The Gaussian invariance principle}

We first replace the product of Boolean slices by a Gaussian space. The \textit{invariance principle} of Mossel, O'Donnell and Oleszkiewicz~\cite{mossel2005noisestabilityfunctionslow} says that a low-degree polynomial of independent bounded random variables has a distribution close to that of the same polynomial evaluated on independent standard Gaussians. Applying this to $L_{\text{err}} + Q$ produces a quadratic polynomial
\begin{equation}
\label{eq:gaussian-quadratic}
  f(\vec Z) = \vec Z^{\top} F \vec Z + \vec f \cdot \vec Z + f_0, \qquad \vec Z = (Z_1, \dots, Z_n) \sim \mathcal{N}(0, I_n),
\end{equation}
with a symmetric matrix $F \in \R^{n \times n}$ and $\vec f \in \R^n$ derived from the adjacency matrix $A$ of $G$ and the bucket partition, such that the characteristic functions of $L_{\text{err}} + Q$ (given $\vec\Delta$) and of $f(\vec Z)$ are close -- see \cite[Lemma~11.1]{Kwan_2023}. The role of this step is to convert a discrete, dependent domain into a continuous, independent one, allowing us to apply the \textit{Carbery--Wright theorem}.

\paragraph{Ingredient 2. The sharpened Carbery--Wright theorem}

Given the $f(\vec Z)$ from ingredient~1, we now bound $\sup_x \prob[|f(\vec Z) - x| \leq 1]$. Recall that the \textit{Frobenius norm} for matrix $M \in \R^{n \times n}$ is $\|M\|_{\mathrm{F}} \coloneqq \left(\sum_{i, j} M_{ij}^2\right)^{1/2}$. In our application $\sigma(f(\vec Z)) = \Omega(n)$, as $\sigma(f(\vec Z))^2 = 2\|F\|_{\mathrm{F}}^2 + \|\vec f\|_2^2$ by \cite[Lemma~11.1(2)]{Kwan_2023} and Ingredient~3 below forces $\|F\|_{\mathrm{F}}^2 = \Omega(n^2)$. The target $O(n^{-1})$ in~\eqref{eq:conditional-target} is therefore asking for a bound of the form $1/\sigma(f(\vec Z))$.

The classical result in this direction is the Carbery--Wright theorem, which states that for quadratic polynomials $f$ in independent standard Gaussians and any $\eps > 0$,
\[
  \sup_{x \in \R} \prob\left[|f(\vec Z) - x| \leq \eps\right] = O\left(\sqrt{\eps / \sigma(f(\vec Z))}\right),
\]
which falls short of our target $O\bigl(1/\sigma(f(\vec Z))\bigr)$. The square-root exponent cannot be improved in general, but it turns out that forcing the associated matrix $F$ to have \textit{effective rank} at least three yields the desired bound.

\begin{theorem}[Sharpened Carbery--Wright, {\cite[Theorem~1.6]{Kwan_2023}}]
\label{thm:sharp-CW}
  Let $f(\vec Z) = \vec Z^{\top} F \vec Z + \vec f \cdot \vec Z + f_0$ be a real quadratic polynomial in independent standard Gaussians, with $F$ a nonzero symmetric matrix. Suppose that for some $\eta > 0$,
  \begin{equation}
  \label{eq:robust-rank-2}
    \min_{\substack{\widetilde F \in \R^{n \times n} \\ \mathrm{rank}(\widetilde F) \leq 2}} \|F - \widetilde F\|_{\mathrm{F}}^2 \geq \eta \|F\|_{\mathrm{F}}^2.
  \end{equation}
  Then for $\eps > 0$,
  \[
    \sup_{x \in \R} \prob\left[|f(\vec Z) - x| \leq \eps\right] = O\left(\eps/\sigma(f(\vec Z))\right).
  \]
\end{theorem}

By the Eckart--Young theorem, the left-hand side of \eqref{eq:robust-rank-2} equals $\sum_{i \geq 3} \sigma_i(F)^2$, where $\sigma_1(F) \geq \sigma_2(F) \geq \cdots \geq \sigma_n(F) \geq 0$ are the singular values of $F$. Condition \eqref{eq:robust-rank-2} therefore asserts that the rank-$(\geq 3)$ tail of $F$ carries at least $\eta$-fraction of $\|F\|_{\mathrm{F}}^2$. 

It remains to show \eqref{eq:robust-rank-2} for the matrix $F$ arising from the bucket structure, and this is where the Ramsey hypothesis enters.

\paragraph{Ingredient 3. The robust rank lemma}

\begin{lemma}[Robust rank, {\cite[Lemma~10.1]{Kwan_2023}}]
\label{lem:robust-rank}
  Fix $C > 0$, $0 < \delta < 1$ and $r \in \N$. Let $G$ be a $C$-Ramsey graph on $n$ vertices with adjacency matrix $A$, and let $V(G) = B_1 \sqcup \cdots \sqcup B_m$ be a partition into equal parts with $n^{\delta/2} \leq m \leq 2n^{\delta}$. Then for every matrix $M \in \R^{n \times n}$ with $\mathrm{rank}(M[B_j \times B_k]) \leq r$ for all $j, k \in [m]$,
  \begin{equation}
    \label{eq:robust}
    \|A - M\|_{\mathrm{F}}^2 = \Omega(n^2).
  \end{equation}
\end{lemma}

The lemma says that the adjacency matrix of a Ramsey graph is at Frobenius distance $\Omega(n)$ from every matrix whose restriction to each bucket block has low rank. When $M$ is binary, $\|A - M\|_{\mathrm{F}}^2$ counts the number of entries on which $A, M$ disagree, so the lemma says no binary matrix with low-rank blocks can agree with $A$ on almost all entries.

In the application of Theorem~\ref{thm:sharp-CW}, the parts $B_j$ are the buckets from~\eqref{eq:partition} of size $\lceil n^{1-2\gamma} \rceil$. Write $\vec x \coloneqq (x_v)_{v \in V}$. The matrix $F$ from Ingredient~1 has the property that, conditional on $\vec \Delta$, the quadratic form $\vec x^{\top} F \vec x$ only concerns the deviation of $\vec x$ from its bucket averages. Specifically, the adjacency matrix $A$ of $G[V \setminus R]$ may be written as
\[
  A = \alpha F + B
\]
for some fixed $\alpha$ and correction matrix $B$ satisfying $\mathrm{rank}(B[B_j \times B_k]) \leq b$ for any block $B_j \times B_k$ and some constant $b$. Suppose for contradiction that some $\tilde{F}$ with rank $\leq 2$ violates~\eqref{eq:robust-rank-2}. Set $\tilde{A} \coloneqq \alpha \tilde{F} + B$. Then for $\eta > 0$, since $\|F\|_{\mathrm{F}}^2 = O(n^2)$,
\[
  \|A - \tilde A\|_{\mathrm{F}}^2 = |\alpha|^2 \cdot \|F - \tilde F\|_{\mathrm{F}}^2 < \eta \cdot |\alpha|^2 \cdot \|F\|_{\mathrm{F}}^2 = O(\eta n^2).
\] 
But then on every $B_j \times B_k$ we have $\mathrm{rank}(\tilde{A}[B_j \times B_k]) \leq b + 2$, contradicting \eqref{eq:robust} for small enough $\eta$. Hence \eqref{eq:robust-rank-2} holds for some $\eta = \eta(C, \gamma)$. See~\cite[Section~12]{Kwan_2023} for the explicit construction of $F$.

To sum it up, applying Ingredients~1, 3, and 2 in sequence yields \eqref{eq:conditional-target}.

\subsection{The switching argument}
\label{sec:switching}

The first stage delivers the interval estimate 
\[
\prob\bigl[|X - x| \leq B\bigr] = \Theta(n^{-3/2}),
\]
but leaves open the worst case where all of this mass sits on some single integer $x' \ne x$ inside the window $[x - B, x + B]$. We rule this out via a \textit{switching argument} between different possibilities of the set $U$, which upgrades the interval estimate to a pointwise bound. The interval estimate \eqref{eq:interval-estimate} already implies an upper bound on $\prob[X = x]$, so it remains to show a lower bound.

For $y \in U$ and $z \notin U$, the \emph{switch} $(y, z)$ replaces $U$ by $U' = (U \setminus \{y\}) \cup \{z\}$. This operation shifts $X$ by
\[
  s(y, z; U) \coloneqq e(U') - e(U) = \deg_{U \setminus \{y\}}(z) - \deg_{U}(y).
\]
Fix a set $T \subseteq V(G)^2$ of candidate ordered pairs, to be specified later, and let
\[
  Y_\ell \coloneqq \#\bigl\{(y, z) \in T : y \in U,\ z \notin U,\ s(y, z; U) = \ell\bigr\}
\]
count candidate switches at $U$ that shift $X$ by exactly $\ell \in \{-B, \ldots, B\}$. Each switch $(y, z) \in T$ with $y \in U, z \notin U$, and $s(y, z; U) = \ell$ pairs $U$ with the set $U' = (U \setminus \{y\}) \cup \{z\}$. The reverse switch $(z, y) \in T$ from $U'$ recovers $U$ and shifts $X$ by $-\ell$. In particular, pairing $(U, (y, z))$ with $(U', (z, y))$ in this way is a bijection. Since the uniform measure on $\{0,1\}^n$ assigns the same probability $2^{-n}$ to every $U$, the bijection gives the symmetry identity
\begin{equation}
  \label{eq:switching-identity}
  \E\bigl[Y_\ell \cdot \mathbbm{1}_{X = x}\bigr] = \E\bigl[Y_{-\ell} \cdot \mathbbm{1}_{X = x + \ell}\bigr],
\end{equation}
for any $x \in \Z$ and $\ell \in \{-B, \ldots, B\}$. By Cauchy--Schwarz,
\[
  \bigl(\E\bigl[Y_{\ell} \cdot \mathbbm{1}_{X = x}\bigr]\bigr)^2 \leq \E\bigl[Y_\ell^2 \cdot \mathbbm{1}_{X = x}\bigr] \cdot \E\bigl[\mathbbm{1}_{X = x}\bigr],
\]
and applying \eqref{eq:switching-identity} yields
\begin{equation}
  \label{eq:CS-main}
  \prob[X = x] = \E\bigl[\mathbbm{1}_{X = x}\bigr] \geq \frac{\bigl(\E\bigl[Y_{-\ell} \cdot \mathbbm{1}_{X = x + \ell}\bigr]\bigr)^2}{\E\bigl[Y_\ell^2 \cdot \mathbbm{1}_{X = x}\bigr]},
\end{equation}
reducing the problem to bounding the numerator and denominator using the interval estimate \eqref{eq:interval-estimate}. For a suitable $T$, Lemma~13.4 of~\cite{Kwan_2023} converts the interval estimate \eqref{eq:interval-estimate} to moment estimates for $Y_\ell$,
\begin{equation}
  \label{eq:moment-estimates}
  \E\left[\prod_{\ell=-B}^B Y_\ell^{a_\ell} \cdot \mathbbm{1}_{|X - x| \leq B}\right] = \Theta\left(\bigl(|T|/\sqrt n\bigr)^{\sum_\ell a_\ell} \cdot n^{-3/2}\right),
\end{equation}
for $a_\ell \in \{0,1,2\}$. Below we apply \eqref{eq:moment-estimates} to bound both numerator and denominator in \eqref{eq:CS-main}.

Since $\mathbbm{1}_{X = x} \leq \mathbbm{1}_{|X - x| \leq B}$, \eqref{eq:moment-estimates} bounds the denominator of \eqref{eq:CS-main} by
\begin{equation}
  \label{eq:den-bound}
  \E\bigl[Y_\ell^2 \cdot \mathbbm{1}_{X = x}\bigr] \leq \E\bigl[Y_\ell^2 \cdot \mathbbm{1}_{|X - x| \leq B}\bigr] \lesssim \left(\frac{|T|}{\sqrt n}\right)^2 \cdot n^{-3/2}.
\end{equation}
As for the numerator, Cauchy--Schwarz yields
\[
  \E\bigl[Y_{-\ell} \cdot \mathbbm{1}_{X = x + \ell}\bigr] \geq \frac{\left(\E\left[\prod_{\ell=-B}^B Y_\ell \cdot \mathbbm{1}_{X = x + \ell}\right]\right)^2}{\E\Big[\prod_{i\ne -\ell}Y_i^{2}\cdot Y_{-\ell} \cdot \mathbbm{1}_{X = x + \ell}\Big]}.
\]
By writing $\mathbbm{1}_{|X - x| \leq B} = \sum_{\ell' = -B}^{B} \mathbbm{1}_{X = x + \ell'}$ and pigeonholing on the largest term, there exists $\ell^* \in \{-B, \ldots, B\}$ such that
\begin{equation}
  \label{eq:moment-1}
  \E\left[\prod_{\ell=-B}^B Y_\ell \cdot \mathbbm{1}_{X = x + \ell^*}\right] \geq \frac{1}{2B+1}\E\left[\prod_{\ell=-B}^B Y_\ell \cdot \mathbbm{1}_{|X - x| \leq B}\right] \gtrsim \left(\frac{|T|}{\sqrt n}\right)^{2B + 1} \cdot n^{-3/2}.
\end{equation}
On the other hand, for all $\ell \in \{-B, \ldots, B\}$, \eqref{eq:moment-estimates} gives
\begin{equation}
  \label{eq:moment-2}
  \E\left[\prod_{i\neq -\ell}Y_i^{2}\cdot Y_{-\ell} \cdot \mathbbm{1}_{X = x + \ell}\right] \leq \E\left[\prod_{i\neq -\ell}Y_i^{2}\cdot Y_{-\ell} \cdot \mathbbm{1}_{|X - x| \leq B}\right] \lesssim \left(\frac{|T|}{\sqrt n}\right)^{4B + 1} \cdot n^{-3/2}.
\end{equation}
Combining \eqref{eq:moment-1} and \eqref{eq:moment-2} gives
\begin{equation}
  \label{eq:num-bound}
  \E\bigl[Y_{-\ell^*} \cdot \mathbbm{1}_{X = x + \ell^*}\bigr] \gtrsim \bigl(|T| / \sqrt{n}\bigr) \cdot n^{-3/2},
\end{equation}
for some $\ell^* \in \{-B, \ldots, B\}$. Thus, \eqref{eq:num-bound} and \eqref{eq:den-bound} give the desired pointwise bound
\[
  \prob[X = x] \gtrsim \frac{\bigl((|T| / \sqrt{n}) \cdot n^{-3/2}\bigr)^2}{\bigl(|T| / \sqrt{n}\bigr)^{2} \cdot n^{-3/2}} = n^{-3/2}.
\]

It remains to find the suitable set $T$ that enables~\eqref{eq:moment-estimates}. The two factors in~\eqref{eq:moment-estimates} have concrete meanings: $n^{-3/2}$ is the interval estimate~\eqref{eq:interval-estimate}, whereas $\E[Y_\ell] = \Theta(|T|/\sqrt n)$, as follows.

For $(y, z) \in T$, we have $\prob[y \in U, z \notin U] = 1/4$. Conditional on this event, set $R_w \coloneqq 2\mathbbm{1}_{w \in U} - 1$ and 
\[
  a_w \coloneqq \mathbbm{1}_{w \in N(z) \setminus N(y)} - \mathbbm{1}_{w \in N(y) \setminus N(z)}.
\]
Then $s(y, z; U)$ may be written as
\begin{align*}
  s(y, z; U)
  &= \deg_{U \setminus \{y\}}(z) - \deg_{U}(y) \\
  &= \sum_{w \in N(z),\, w \neq y} \mathbbm{1}_{w \in U} - \sum_{w \in N(y)} \mathbbm{1}_{w \in U} \\
  &= \frac{\deg(z) - \deg(y)}{2} + \frac{1}{2}\sum_{w \in N(y) \triangle N(z)} a_w R_w,
\end{align*}
which is of $(|N(y) \triangle N(z)|, 1/2)$-Littlewood--Offord type. Now suppose we can find $T$ such that for $(y, z) \in T$
\begin{enumerate}
  \item $|N(y) \triangle N(z)| = \Theta(n)$,
  \item $|\deg(z) - \deg(y)| = O(\sqrt{n})$
\end{enumerate}
Condition~1 controls the variance of $s(y, z; U)$ at $\Theta(n)$, and condition~2 places the conditional mean $(\deg(z) - \deg(y))/2$ within $O(\sqrt n)$ of zero, so $\ell \in \{-B, \ldots, B\}$ falls within $O(\sqrt n)$ around the mean. Erd\H{o}s--Littlewood--Offord then gives the upper bound
\[
  \prob\bigl[s(y, z; U) = \ell \mid y \in U, z \notin U\bigr] = O(1/\sqrt n)
\]
uniformly in $\ell$, and Stirling's formula in the form $\binom{m}{m/2 + k} 2^{-m} = \Theta(m^{-1/2})$ for $|k| = O(\sqrt m)$ gives the matching lower bound 
\[
  \prob\bigl[s(y, z; U) = \ell \mid y \in U, z \notin U\bigr] = \Omega(1/\sqrt n)
\]
for $\ell$ around the mean. Hence, $\prob[s(y, z; U) = \ell \mid y \in U, z \notin U] = \Theta(1/\sqrt n)$ for $\ell \in \{-B, \ldots, B\}$, and so
\[
  \E[Y_\ell] = \frac{1}{4}\sum_{(y, z) \in T} \prob\bigl[s(y, z; U) = \ell \mid y \in U, z \notin U\bigr] = \Theta\bigl(|T|/\sqrt n\bigr).
\]
It remains to construct such a set $T$ and to prove~\eqref{eq:moment-estimates}. Both are carried out inside a linear-sized $(\eps, \delta)$-rich induced subgraph $S_0 \subseteq V(G)$. Condition~1 follows from two applications of richness inside $S_0$, condition~2 follows from pigeonholing. The proof of~\eqref{eq:moment-estimates} further relies on a \textit{dependent random choice} technique which supplies $S_0$ with a strong common-neighborhood property needed to control correlations between switches of multiple pairs in $T$ -- see \cite[Lemmas~13.1, 13.3, 13.4]{Kwan_2023} for details.